# だれでも軌道設計 - プロジェクト概要

公開URL: https://edvega8823.github.io/Anyone_trajectory_design/

## これは何か

ブラウザだけで動く**惑星探査機の軌道設計ツール**。太陽系を3Dで表示し、
「打上げ → スイングバイ → 周回軌道投入 → 軌道脱出 → 大気圏突入」といったミッションシーケンスを
対話的に組み立てながら、遷移軌道 (ランベール問題の解) を計算・可視化する。
惑星 (水星〜冥王星) に加え、MPC の目録から取り込んだ小惑星・彗星・恒星間天体も目的地にできる。

日本語UIで、教育・一般向け (だれでも軌道設計できる) を意図している。
ビルド工程は無く、GitHub Pages が静的ファイルをそのまま配信している。

## コンセプト

目指すのは、**スイングバイを含む惑星間遷移軌道の設計を、人が手で組み立てられるようにすること**。

これは軌道力学で言う **MGA (Multiple Gravity Assist)** や **MGA-1DSM (MGA with 1 Deep Space Maneuver)**
というミッションシーケンス表現に相当する。`pykep` (ESA発の軌道力学Pythonライブラリ) などでは通常、
遺伝的アルゴリズムのような大域最適化ソルバに投げて自動探索させる問題として扱われる。

本プロジェクトはその**自動探索を、人間の手作業・直感操作に置き換える**:

- MGA/MGA-1DSM の設計変数 (各天体の通過日時、スイングバイの近点高度・回転角、DSM の位置とΔV など) を、
  ソルバに探しに行かせるのではなく、3D空間でノードをドラッグしたり数値を打ち込んだりして人が決める
- `Mission` クラス (`trajectory.js`) が持つシーケンス配列 (`#m_planet_nums` / `#m_dates` / `#m_types`) が、
  MGA/MGA-1DSM の「訪問天体列 + 各天体の通過時刻」というシーケンス表現そのものに対応する

ソルバを完全に排除しているわけではない。「自動調整」(`optimize.js`) は載っているが、
**大域探索ではなく、人が作った設計のすぐ近くを局所的に磨くだけ**の位置づけにしてある
(出発点は現在の設計ひとつ、変数の範囲も初期値のまわりに狭く取る)。
別物の軌道を返してきては「調整」にならず、手で作るというコンセプトが壊れるため。

## ミッションシーケンスの節

`Sequence_Type` (`state.js`) の一覧。天体に応じて選べる種別が変わる (惑星ならスイングバイ・周回軌道投入、
小天体ならフライバイ・ランデブー)。

| 種別 | 中身 |
|---|---|
| 打上げ | 先頭の節。地球などを出発する。V∞ の向きと大きさを持つ |
| スイングバイ | 惑星の重力で曲がる。自動/手動の2モード (下記) |
| フライバイ | 小天体を通り過ぎる。重力が小さすぎて曲がらないので、速度差はすべて自前のΔV |
| ランデブー | 小天体に速度を合わせて並走する |
| 再出発 | ランデブーした小天体から飛び立つ。自動/手動の2モード |
| 周回軌道投入 | 惑星の周回軌道に入る (捕獲)。近点・遠点高度が設計変数 |
| 軌道脱出 | 周回軌道から再び飛び立つ。自動/手動の2モード |
| 大気圏突入 | 大気に突入して終える。突入角γのみが設計変数 (突入速度はエネルギーで決まる) |
| マヌーバ | 深宇宙での噴射 (DSM)。手動モードの節に自動で付いてくるほか、手で足すこともできる |
| 最終軌道 | 目的地を持たずに終える。到達した太陽中心軌道そのものが成果 |

### 自動 / 手動 (MGA と MGA-1DSM)

「天体を離れる」性格の節 (打上げ・スイングバイ・軌道脱出・再出発) は自動/手動を選べる。

| モード | 中身 | 次の天体への到達 |
|---|---|---|
| 自動 (MGA) | 前後2本のランベール弧を接続する。向きの差はスイングバイの幾何 (近点高度・回転角β) で、大きさの差は**近点ΔV** (パワード・フライバイ) で吸収する。これらは計算結果であって入力ではない | 保証される (近点ΔVを払って) |
| 手動 (MGA-1DSM) | ユーザーが V∞ や近点高度・βを直接与えて無推力で飛ぶ。直後に**マヌーバ(DSM)ノードが自動で挿入され**、そこから次の天体までをランベールで解く | 保証される (DSMのΔVを払って) |

手動モードで挿入される DSM の**位置**は、そのマヌーバノードの日付で決まる (選んで時刻を動かせる／太陽系ビューで
ドラッグできる)。DSM のΔVそのものはランベール解で決まる従属量で、画面では「この後の軌道修正」と呼んでいる。
これが小さく収まるほど、手前の設定が的を射ているということになる。

最終軌道で終えている間は、その先に目的地が無いので自動には戻せない (手動のみ)。

## 技術スタック

- 素の HTML/CSS/JavaScript (ES モジュール)。ビルドツールもフレームワークも無し
- **three.js** (r142, CDN) — 3D描画、`OrbitControls`、`CSS2DRenderer` (ラベル)
- **math.js** (`js/lib/math.js`) — ベクトル演算
- **lambert-orbit** (`js/lib/lambert_probrem.js`) — ランベール問題ソルバ (Izzo 2015)。
  精度検証の上で3か所を修正済み (`a6fcf2e`)。
  多周回の最短飛行時間を直接求める `lambert_tof_min` を追加した (`trajectory.js` の `lambert_min_tof` が包む)
- 打上げ能力の表と補間は `pykep` からの移植 (`js/launchers.js`)
- 小天体データは MPC 由来 (`data/bodies/`、GitHub Actions で月1更新)
- Google Analytics (gtag.js)

## ファイル構成

```
index.html              全体のDOM構造 (太陽系ビュー + シーケンス一覧 + 操作パネル)
css/elements.css        スタイル定義 (縦長・スマホ幅のレイアウトもここ)
PROJECT_OVERVIEW.md     このファイル

js/
  state.js              全体状態 (State, PlotState) と節の種別 (Sequence_Type)
  trajectory.js         軌道力学の計算コアと Mission クラス
  event.js              入力の受け口 (一覧の操作、日時、3Dビューのドラッグ)
  main.js               UIのオーケストレーション。操作パネル各節の描画もここ
  history.js            元に戻す / やり直す (設計を丸ごとスナップショット)
  mission_file.js       ミッションの保存と読込 (JSON)
  share_link.js         共有URLの生成・復元と、Xへの投稿
  export_image.js       設計を1枚のPNGにする (太陽系ビュー + 成績 + シーケンス)
  topbar.js             画面上部のバーとメニュー、通知トースト
  dialog.js             はい/いいえを尋ねる小さな画面

  plot.js               太陽系ビュー (three.js)。軸・グリッド・天体・軌道線・カメラ
  orbit_pick.js         太陽系ビューで軌道の線を掴むための当たり判定
  view3d.js             操作パネルの小さな3Dビューの共通部品
  vector_view.js        「大きさ + 2つの角度」のベクトルをマウスで操るひな型
  vinf_view.js          天体を離れる瞬間の相対速度を見る遠景ビューのひな型
  launch_view.js          打上げ (vinf_view のひな型を使う)
  escape_view.js          軌道脱出の遠景 (同上)
  departure_view.js       再出発 (同上)
  bplane.js             スイングバイのB面ビュー
  orbit_view.js         周回軌道投入 / 軌道脱出の近景ビュー
  entry_view.js         大気圏突入のビュー
  dsm_view.js           手動マヌーバのビュー

  porkchop.js           ポークチョップ図 (出発日と到着日の総当たり)
  opt_problem.js        自動調整の定式化 (目的関数・設計変数・範囲)
  optimize.js           自動調整のソルバ (有限差分の勾配 + Nelder-Mead)
  launchers.js          打上げ能力の表と補間 (pykep 移植)
  bodies.js             小天体データの読み込み
  small_bodies.js       取り込んだ小天体の置き場
  body_picker.js        天体を追加する画面
  lib/math.js           外部ライブラリ
  lib/lambert_probrem.js 外部ライブラリ (ランベール問題ソルバ)

data/bodies/            小天体の軌道要素 (index.json + まとまりごとのJSON)。README.md に仕様
tools/build_bodies.py   MPC の配布ファイルから data/bodies/ を作る
tools/check_bodies.py   作ったデータの検査
textures/hipp8.jpg      (現状未使用)
```

## 主要な仕組み

### 軌道力学 (`trajectory.js`)
- 惑星の軌道要素 (a, e, i, 平均黄経L, 近日点黄経ϖ, 昇交点黄経Ω) を J2000.0 元期からの線形近似で時刻から算出
  (`element_0` + `element_dot`)。小天体は元期の要素を伝播する
- ケプラー方程式を数値的に解く。離心近点角⇔真近点角、状態ベクトル⇔軌道要素 (`ic2par`) の変換
- **`Mission` クラス**: シーケンス (訪問天体・日時・種別) を private フィールドで持ち、
  天体位置・探査機の位置速度・遷移軌道弧を一括で再計算する。設定をまとめて変えるための `batch()`、
  隣接ノードとの最小間隔 (`MIN_NODE_GAP` = 10日)、周回数と分枝 (low/high) の選択、
  `leg_arrival_vinf()` (この先どの天体にどれだけの速さで着くか) などを持つ
- ロケット方程式で最終質量を出す (`final_mass`、`SPACECRAFT_ISP` = 320秒)

### 太陽系ビュー (`plot.js` / `event.js` / `orbit_pick.js`)
- 天体やシーケンスのノードを**ドラッグすると時刻が変わる**。画面上で軌道の線に吸い付かせている
- Z拡大 (黄道面からの高さだけを引き伸ばす) で軌道の傾きを見る
- 選択中の節に応じて、関係する天体の公転軌道だけを出す

### 操作パネルの3Dビュー
節ごとに独立した小さなシーンを持つ (`view3d.js` が共通部品)。
入力欄を選ぶとビューに**マウスで掴めるハンドル**が出て、数値と図のどちらからでも同じ量を動かせる。

### ポークチョップ図 (`porkchop.js`)
自動モードの区間について、出発日と到着日を総当たりでランベールに掛け、打上げエネルギーなどを色で塗る。
多周回に対応 (点ごとに一番安い周回数を採るか、周回数を固定するか)。図の点を押すとその日付になる。

### 自動調整 (`opt_problem.js` / `optimize.js`)
- **最終質量の最大化**。打上げ能力は V∞ が大きいほど落ちるので、「速く出る」ことと「燃料を使わない」ことの
  綱引きがそのまま1つの数になる (総ΔVだけを見るのでは足りない)
- 設計変数はすべて [0,1] の箱に写す。桁で効く量は対数で、時刻は「打上げ日 + 各区間の飛行時間」で持つ
  (順序の制約が箱の下限に化けて消える)
- 周回軌道の高度は**動かさない** (観測要件であって燃料の都合で変えるものではない)。
  周回数・分枝は連続でないので探索の外
- 有限差分の勾配 (Barzilai-Borwein) と Nelder-Mead を交互に回す。後者は |ΔV| が v=0 で
  微分できない折れ点を跨ぐ担当。`requestAnimationFrame` で細切れに走らせて画面を固めない

### 保存・共有 (`mission_file.js` / `share_link.js` / `export_image.js`)
- 保存は JSON ファイル。読込はファイル選択とドラッグ&ドロップの両方
- 共有URLは、設計を `CompressionStream('deflate-raw')` で圧縮して base64url にし、URL の断片 (`#`) に載せる。
  置き場所を持たない静的サイトなので、設計そのものをURLに埋め込んでいる
- 画像は画面の写真ではなく、載せたいもの (太陽系ビュー・成績・シーケンス) を1400px幅の紙面に並べ直して描く
- 元に戻す/やり直す (`history.js`) は、操作ではなく**設計そのものを丸ごと**覚える (数キロバイトのJSON)

## 画面の言葉づかい

「だれでも」と名乗る以上、画面に出す文字は**それを知らない人が読んで分かる言葉**にする。

- 業界の略語を画面に出さない: DSM →「この後の軌道修正」、レグ → 区間、ノード → シーケンス、
  無推力 → 噴射なし、侵入速度 → 近づく速さ
- 説明 (ツールチップ) には、なぜそうなるかの導出ではなく、**何が起きるか・何を決めればいいか・
  どちらへ動かすと楽になるか**を書く。3行程度に収める
- 数値の並べすぎを避ける。設計を決めるのに要るものだけを出し、確認にしか使わないものは
  「高度な情報」に畳む (`makeAdvanced` / `fillTiles`)

ただし**設計に効く量は畳まない**。たとえばスイングバイの V∞ は曲げられる角度を決めている当の数字なので、
常に出したうえ、そこへ向かう区間 (打上げ・マヌーバ・再出発・軌道脱出・区間の周回数) にも
「金星での V∞」として出す。V∞ は着く前の飛び方で決まってしまう量で、スイングバイの節を開いてからでは遅いため。
同じ天体へ何度も戻って V∞ を育てる設計 (VILM) では、これ自体が区間の目標になる。

## いまの状態

実装済み: 上に挙げた節・モード・ビューはすべて動く。保存/読込 (Ctrl+S / Ctrl+O)、
元に戻す/やり直す (Ctrl+Z / Ctrl+Y)、共有リンク、Xへの投稿、画像で保存、自動調整、
ポークチョップ図、天体の追加。

まだ無いもの (押すと「準備中です」と出る):

| | |
|---|---|
| 例を読み込む | 実際の探査機のミッションを読み込んで参考にする |
| キーボード操作 | ショートカットの一覧 |
| 表示設定 | 単位や配色 |
| フィードバックを送る | 要望・不具合の受け口 |
| ヘルプ | 使い方のドキュメント |
| EN (英語表示) | バーに枠だけある。切り替えると「準備中です」と出る |

テストの仕組みはリポジトリに入っていない。動作確認は、ローカルの静的サーバに向けた
ヘッドレスブラウザ (CDP) で画面を操作し、表示と数値を読み出す形で都度行っている。

## データの更新

`data/bodies/` は `tools/build_bodies.py` が MPC の配布ファイルから作り、GitHub Actions
(`.github/workflows/update-bodies.yml`) が毎月1日に走らせる。**手で編集しない。**
詳しくは `data/bodies/README.md`。

## 用語

- **V∞ (V-infinity)**: 天体の重力を振り切った後に残る速度。画面では場面に応じて「脱出速度」
  「近づく速さ」「入ってくる V∞」などと呼び分けている
- **C3**: 特性エネルギー (V∞ の2乗)。打上げロケットの能力指標。画面では「打上げエネルギー」
- **ΔV**: 探査機が自分の推進で出す速度変化。燃料を食う量そのもの
- **DSM (Deep Space Maneuver)**: 天体から離れた宇宙空間での噴射。画面では「マヌーバ (深宇宙での噴射)」、
  手動モードに自動で付くものは「この後の軌道修正」
- **ランベール問題**: 2点の位置と飛行時間から、それを結ぶ軌道の速度を求める問題。遷移軌道設計の基本
- **パワード・フライバイ**: スイングバイの近点で噴射して、重力だけでは足りない曲げを補うこと
- **VILM (V∞ Leveraging Maneuver)**: 深宇宙で噴いてから同じ天体へ戻り、V∞ を育てる手法
