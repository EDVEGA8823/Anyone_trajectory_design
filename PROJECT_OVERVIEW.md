# だれでも軌道設計 - プロジェクト概要

公開URL: https://edvega8823.github.io/Anyone_trajectory_design/

## これは何か

ブラウザだけで動く**惑星探査機の軌道設計シミュレータ**。太陽系内の惑星(水星〜冥王星)の軌道を3D表示し、
「打上げ→スイングバイ→フライバイ→周回軌道投入/ランデブー」といったミッションシーケンスを
ユーザーが対話的に組み立てながら、探査機の軌道(ランベール問題を解いた遷移軌道)を計算・可視化できるツール。
日本語UIで、教育・一般向け(誰でも軌道設計できる)を意図している。

## 最終目標・設計方針

本プロジェクトが目指す最終形は、**スイングバイを含む惑星間遷移軌道の設計を、ユーザーが手動で対話的に行えるようにすること**。
これは軌道力学分野で言う **MGA (Multiple Gravity Assist)** や **MGA-1DSM (MGA with 1 Deep Space Maneuver)** という
ミッションシーケンス表現に相当し、`pykep`(ESA発の軌道力学Pythonライブラリ)などでは通常、
遺伝的アルゴリズム等の最適化ソルバーに投げて自動探索させる問題として扱われる。

本プロジェクトはその**自動最適化を人間の手作業・直感操作に置き換える**ことをコンセプトとしている:

- MGA/MGA-1DSMにおける設計変数(各天体でのフライバイ日時、スイングバイ時のパラメータ、
  DSM=Deep Space Maneuverの位置・Δv など)を、ソルバーに探索させるのではなく、
  3D空間上で惑星やシーケンスノードをドラッグ操作することでユーザー自身が「勘」で調整できるようにする
- `Mission`クラス(`trajectory.js`)のシーケンス配列(`#m_planet_nums`, `#m_dates`, `#m_types`)が、
  MGA/MGA-1DSMにおける「訪問天体列 + 各天体通過(または出発)時刻」というシーケンス表現そのものに対応する
- `Sequence_Type`に`Swingby`(スイングバイ)や`Maneuver`(マヌーバ)がすでに定義されているのは、
  この最終形(MGA-1DSM相当の手動設計)を見据えたもの

### 実装状況とモードの定義

スイングバイノードには「自動」「手動」の2モードがある(`Mission.set_auto_mode`):

| モード | 中身 | 次の天体への到達 | 状態 |
|---|---|---|---|
| 自動 | 前後2本のランベール弧を接続。向きの差はスイングバイの幾何(rp・β)で、大きさの差は**近点ΔV**(パワード・フライバイ)で吸収する。rp・β・ΔVは計算結果(表示用) | 保証される(近点ΔVを払って) | 実装済み |
| 手動 | ユーザーが指定したrp・βで双曲線に沿って曲げるだけ。純粋な無推力フライバイ | 保証されない(ユーザーが手で詰める) | 実装済み |
| MGA-1DSM | レグ途中の指定位置(レグの割合ηが設計変数として増える)でDSMを打ち、**DSM以降をランベールで解いて**次の天体に繋げる | 保証される(DSMのΔVを払って) | **未実装** |

MGA-1DSMは「手動モード」とは別物である点に注意。手動モードは単に曲げっぱなしにするモードであり、
MGA-1DSMはDSMという追加の推進機会によって正確な到達を実現する方式。

つまり「だれでも軌道設計」というタイトルは、専門的な最適化ソルバー(pykepのMGA/MGA-1DSM問題)が要求するような
軌道設計を、専門知識やソルバーなしに、直感的なGUI操作だけで誰でも組み立てられるようにする、という
プロジェクトの核心的なモチベーションを表している。

## 技術スタック

- 素のHTML/CSS/JavaScript (ESモジュール)。ビルドツールやフレームワークなし
- **three.js** (r142, CDN読み込み) — 3D描画、`OrbitControls`、`CSS2DRenderer`(ラベル表示)
- **math.js** (`js/lib/math.js`) — ベクトル演算等
- **lambert-orbit** (`js/lib/lambert_probrem.js`) — ランベール問題ソルバー(2点間の位置と飛行時間から遷移軌道の速度を求める)
- Google Analytics (gtag.js) 埋め込み済み

## ファイル構成

```
index.html            全体のDOM構造(グラフ表示パネル + UI操作パネル)
css/elements.css      スタイル定義
js/
  main.js             起動処理・UI更新のオーケストレーション(シーケンスリスト描画、プロット更新など)
  state.js            グローバル状態(State, PlotState)とシーケンス種別の定数定義
  event.js            ユーザー操作のイベントハンドラ(日時変更、シーケンス追加/選択、
                       マウス/タッチでの惑星ドラッグによる時刻操作など)
  trajectory.js       軌道力学の計算コア(惑星の軌道要素、ケプラー方程式、座標変換、
                       Missionクラス=シーケンス管理、遷移軌道計算)
  plot.js             three.jsによる3D描画(軸・グリッド・惑星・軌道線・カメラ制御・アニメーションループ)
  lib/math.js          外部ライブラリ
  lib/lambert_probrem.js 外部ライブラリ(ランベール問題ソルバー)
textures/hipp8.jpg    (現状未使用と思われるテクスチャ画像)
```

## 主要な仕組み

### 1. 状態管理 (`state.js`)
- `State`: 現在選択中のシーケンス、日時、惑星軌道要素、`Mission`インスタンドなどアプリ全体の状態
- `PlotState`: three.jsのオブジェクト(惑星球体・軌道線・マーカー)とカメラ距離など描画状態
- `Sequence_Type`: 打上げ/スイングバイ/フライバイ/周回軌道投入/ランデブー/マヌーバ

### 2. 軌道力学 (`trajectory.js`)
- 各惑星の軌道要素(a, e, i, Ω, ω, E)を時刻から算出(J2000.0元期からの線形近似モデル、`element_0`/`element_dot`)
- ケプラー方程式を数値的に解く(`solve_kepler`)、離心近点角⇔真近点角変換、周期計算
- 位置・速度ベクトル変換 (`ic2par`: 状態ベクトル→軌道要素の逆変換も実装)
- **`Mission`クラス**: ミッションシーケンス(訪問天体・日時・種別のリスト)をプライベートフィールドで管理し、
  惑星位置・探査機の位置速度・ランベール問題による遷移軌道弧を自動計算するカプセル化されたコア

### 3. UIオーケストレーション (`main.js`)
- シーケンスパネルへのDOM要素追加/更新 (`add_sequence`, `change_sequence`)
- 天体・シーケンス種別のセレクトボックス更新 (`change_sequence_propaty`)
- 全惑星位置・軌道の再計算とプロット更新 (`calc`, `update_plot`)
- 選択中シーケンスに応じた軌道線・マーカーの表示切替 (`toggle_planet`)
- 起動処理 (`boot`) でMission初期化・イベント登録・初期プロット生成

### 4. イベント処理 (`event.js`)
- シーケンスパネルのクリック(追加/選択)
- 日時入力欄の変更、確定/キャンセルボタン
- **3D空間内の惑星をドラッグして日時を直感的に変更する機能**(マウス/タッチ両対応、
  レイキャストで惑星軌道面上の真近点角を求めケプラー方程式で日時に変換、周回数もトラッキング)

### 5. 3D描画 (`plot.js`)
- シーン初期化、座標軸・グリッド線・AU単位ラベルの生成
- 惑星球体・ラベル(CSS2DObject)の生成・更新
- 軌道線・遷移軌道弧の生成/更新(`createLine`/`updateLine`、`BufferGeometry`を再利用)
- カメラ距離に応じたグリッド線・ラベルの不透明度/スケール調整、リサイズ対応
- `requestAnimationFrame`による描画ループ

## 現在の作業状況(git status より)

直近のコミット履歴を見ると、当初 `js/global_var.js` にあったグローバル状態が `js/state.js` へ切り出され、
`main.js`/`event.js`/`plot.js`/`trajectory.js` 全体にわたるリファクタリング(状態管理の整理、
`Mission`クラスによるカプセル化、シーケンスUIの整理、V∞/C₃表示の追加)が進行中。
現在の変更(コミット前)は主にこのリファクタリングの続き:
- `js/global_var.js` 削除 → `js/state.js` に統合
- `Mission`クラスの導入によりシーケンス関連ロジックが集約された
- 直近コミット「変更できる時間に制限を設ける」で、`Mission.set_date`に隣接シーケンスとの最小時間間隔(10日)の制約を追加

## 用語

- **V∞ (V-infinity)**: 打上げ時の無限遠速度(地球重力圏を脱した後の余剰速度)
- **C3**: 特性エネルギー(V∞の二乗)、打上げロケットの能力指標としてよく使われる
- **ランベール問題**: 2点の位置ベクトルと飛行時間から、それらを結ぶ軌道の速度ベクトルを求める問題(遷移軌道設計の基本)
